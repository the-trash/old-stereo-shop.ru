%footer
  .our-products
    %h2
      = t('our_production')
      %span.icons-arrow-black-down
    - if @front_presenter.product_categories.any?
      %ul.menu
        %li= link_to t('sale'), [:sale, :product_categories], class: 'pink'
        = build_product_category_ancestry_tree(@front_presenter.product_categories)

  .row.dark-gray-bg
    .container
      %h2.pull-left
        Поиск
        %span.icons-arrow-black-right
      = render 'shared/search_bar'
  .row.who-are-we
    .container
      .pull-left
        %h2
          Кто мы
          %span.icons-arrow-black-down
        %ul.menu.left-menu
          %li
            = link_to I18n.t('views.pages.about'),
              page_path(:about),
              class: active_class_for_page(params, 'about')
          %li= link_to t('brands'), [:brands]
          %li
            = link_to I18n.t('views.pages.contacts'),
              page_path(:contacts),
              class: active_class_for_page(params, 'contacts')
            .help-block
              %p= @settings[:shop_phone]
              %p.pink= "#{ @settings[:shop_work_start] } - #{ @settings[:shop_work_end] }"
          %li= link_to @news.title, @news if @news
          %li.pull-right= link_to @useful_information.title, @useful_information if @useful_information
      .pull-left.right-block
        .gray-block
          %h2
            Удобство и безопасность
            %span.icons-arrow-black-down
          %ul.menu.right-menu
            = render 'shared/includes/static_pages_links'

        - if user_signed_in?
          = render 'shared/includes/subscribe', email: current_user.email if policy(current_user).subscibe?
        - else
          = render 'shared/includes/subscribe'
