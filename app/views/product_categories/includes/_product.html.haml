.b-single-product.catalog
  - photo = product.photos.first
  - if photo
    .e-image{ style: "background-image: url(#{ photo.file_url(:product, :similar) })" }

  .b-info
    .b-info-main
      %p.title= link_to product.title, product
      - if product.characteristics_products.present?
        %ul.characteristics-list
          = render partial: 'product_categories/includes/characteristic',
            collection: product.characteristics_products, as: :characteristic

    .b-info-short
      = price_with_discount(product.price, product.price_with_discount)
      .b-product-ratable-with-reviews.smal
        .ratable.read_only{ data: { score: product.average_score } }
        = link_to t('views.product.reviews_count', count: product.published_reviews_count), product_path(product, anchor: 'reviews'), class: 'reviews-count'
      %p= icon_in_stock(product, 'small')

    .b-commerce
      %ul
        %li
          = button_to line_items_path(product_id: product.id), class: 'btn add-to-cart single-product' do
            %i.icon.icon-add-to-cart
            = t('add_to_cart')
        %li
          = add_to_wishlist(product) if user_signed_in?
