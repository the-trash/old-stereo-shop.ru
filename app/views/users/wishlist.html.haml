.b-user-wishlist
  %h1= t('my_wishlist')

  - if @wishes.present?
    .b-products
      = render partial: 'products/includes/single_product', collection: @wishes, as: :product

      = will_paginate @wishes
  - else
    Ваш список желаний пока пуст
